{
  "name": "simply-build-task-scsslint",
  "version": "0.0.0",
  "description": "Lint CSS files using the sass-lint package.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Han <support@aureola.codes>",
  "license": "MIT",
  "dependencies": {
    "sass-lint": "^1.7.0"
  },
  "config": {
    "pattern": "src/**/*.scss",
    "ignore": "node_modules"
  }
}
